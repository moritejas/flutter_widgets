import 'package:flutter/material.dart';

class ChoiceChipComponent extends StatelessWidget {
  const ChoiceChipComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(" This is App Bar"),
      ),
    );
  }
}
