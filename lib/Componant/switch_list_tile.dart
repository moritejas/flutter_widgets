import 'package:flutter/material.dart';

class SwitchListTileComponent extends StatelessWidget {
  const SwitchListTileComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(" This is App Bar"),
      ),
    );
  }
}
