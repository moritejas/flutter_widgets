import 'package:flutter/material.dart';

class RattingBarComponent extends StatelessWidget {
  const RattingBarComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(" This is App Bar"),
      ),
    );
  }
}
