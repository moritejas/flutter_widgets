import 'package:flutter/material.dart';

class CheckBoxGroupComponent extends StatelessWidget {
  const CheckBoxGroupComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(" This is App Bar"),
      ),
    );
  }
}
