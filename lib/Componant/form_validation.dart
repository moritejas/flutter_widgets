import 'package:flutter/material.dart';

class FormValidationComponent extends StatelessWidget {
  const FormValidationComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: const Center(
        child: Text("Welcome to Flutter Application "),
      ),
    );
  }
}
