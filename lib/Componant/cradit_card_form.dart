import 'package:flutter/material.dart';

class CreditCardFormComponent extends StatelessWidget {
  const CreditCardFormComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(" This is App Bar"),
      ),
    );
  }
}
