import 'package:flutter/material.dart';

class SwipeableStackComponent extends StatelessWidget {
  const SwipeableStackComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: const Text("This is Column Component"),

    );
  }
}
