import 'package:flutter/material.dart';

class StaggeredViewComponent extends StatelessWidget {
  const StaggeredViewComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
      ),
      body: const Center(
        child: Text("Welcome to Flutter Application "),
      ),
    );
  }
}
