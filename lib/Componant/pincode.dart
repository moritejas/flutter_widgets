import 'package:flutter/material.dart';

class PinCodeComponent extends StatelessWidget {
  const PinCodeComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(" This is App Bar"),
      ),
    );
  }
}
